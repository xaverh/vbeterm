# Tell clangd where to find compile_commands.json (generated by `bear -- make`
# after ./configure, see README).
# If no compile_commands.json is present, clangd will fall back to
# compile_flags.txt in the same or ancestor directories for basic include paths.
CompileFlags:
  CompilationDatabase: .
